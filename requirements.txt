Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0
Flask-Login>=0.6
python-dotenv>=1.0

# Report exports
openpyxl>=3.1      # Excel (.xlsx) export
reportlab>=4.0     # PDF export

# Reserved for upcoming tasks (forms). Uncomment when you reach them:
# Flask-WTF>=1.2

# MySQL / MariaDB driver for production (shared cPanel hosting).
PyMySQL>=1.1

# PostgreSQL driver — only if you move to a host that offers Postgres:
# psycopg2-binary>=2.9
